<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RetroMeal"
        tools:targetApi="s">

        <activity android:name="com.example.retromeal.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <receiver android:name=".SplashActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.retromeal.ACTION_LOAD_COMPLETE"/>
            </intent-filter>
        </receiver>

        <!-- MainActivity -->
        <activity android:name="com.example.retromeal.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme"/>

        <!-- DetailActivity -->
        <activity android:name="com.example.retromeal.DetailActivity"
            android:theme="@style/AppTheme"
            android:exported="true"/>

    </application>

    <uses-permission android:name="android.permission.INTERNET"
        tools:ignore="ManifestOrder" />
</manifest>
